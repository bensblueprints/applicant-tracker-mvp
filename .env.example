# Hirestack configuration — copy to .env and edit

# Port the web app listens on
PORT=5354

# Admin password for the dashboard (change this!)
ADMIN_PASSWORD=change-me

# Company name shown on the public careers page
COMPANY_NAME=Our company

# SQLite database location (resumes stored in an uploads/ dir next to it)
DB_PATH=./data/hirestack.db
